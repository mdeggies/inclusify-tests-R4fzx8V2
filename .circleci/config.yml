### ***
### WARNING: DO NOT manually EDIT or MERGE this file, it is generated by 'make ci-config'.
### INSTEAD: Edit or merge the source in config/ then run 'make ci-config'.
### ***
version: 2
jobs:
  test:
    docker:
    - image: cimg/base:2020.01
    environment:
    - DEFAULT_BRANCH: main-nROX4fWS
    shell: /usr/bin/env bash -euo pipefail
    steps:
    - checkout
    - attach_workspace:
        at: .
    - run:
        command: mkdir main-nROX4fWS
    - run:
        command: echo $DEFAULT_BRANCH
    - run:
        command: echo $BRANCH
    - persist_to_workspace:
        paths:
        - main-nROX4fWS
        root: .
workflows:
  ci:
    jobs:
    - test:
        filters:
          branches:
            ignore: main-nROX4fWS
  version: 2

# Original config.yml file:
# executors:
#   go:
#     docker:
#     - image: golang:1.14.6
#     environment:
#       BRANCH: main-nROX4fWS
#       GOPATH: /go
#     working_directory: /go/src/github.com/mdeggies/circleci-tester
# jobs:
#   test:
#     docker:
#     - image: cimg/base:2020.01
#     environment:
#       DEFAULT_BRANCH: main-nROX4fWS
#     shell: /usr/bin/env bash -euo pipefail
#     steps:
#     - checkout
#     - attach_workspace:
#         at: .
#     - run: mkdir main-nROX4fWS
#     - run: echo $DEFAULT_BRANCH
#     - run: echo $BRANCH
#     - persist_to_workspace:
#         paths:
#         - main-nROX4fWS
#         root: .
# references:
#   common_envs:
#     BRANCH: main-nROX4fWS
#   go-machine-image: circleci/classic:201808-01
# version: 2.1
# workflows:
#   ci:
#     jobs:
#     - test:
#         filters:
#           branches:
#             ignore: main-nROX4fWS